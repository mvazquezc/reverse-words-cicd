apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-words
  annotations:
    kubernetes.io/ingress.class: nginx
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  rules:
  - host: user1-reversewords-dev.apps.cluster-657e.657e.sandbox1315.opentlc.com
    http:
      paths:
      - backend:
          serviceName: reverse-words
          servicePort: http
