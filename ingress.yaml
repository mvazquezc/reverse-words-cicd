apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-words
  annotations:
    kubernetes.io/ingress.class: nginx
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  rules:
  - host: reversewords-prod.apps.demo.5a39.sandbox1183.opentlc.com
    http:
      paths:
      - backend:
          serviceName: reverse-words
          servicePort: http
