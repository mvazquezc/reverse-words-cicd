apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-words
  annotations:
    kubernetes.io/ingress.class: nginx
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  rules:
  - host: reversewords.apps.spoke2.5324.sandbox1807.opentlc.com
    http:
      paths:
      - backend:
          serviceName: reverse-words
          servicePort: http
